<%= form_with(model: conter, local: true) do |form| %>
  <% if conter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conter.errors.count, "error") %> prohibited this conter from being saved:</h2>

      <ul>
        <% conter.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :count %>
    <%= form.number_field :count %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
